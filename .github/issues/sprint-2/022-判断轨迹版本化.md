---
title: '[Sprint 2] 实现判断轨迹版本化（Decision History）'
labels: 'sprint-2,backend,frontend,priority:high'
assignees: ''
---

## 📋 任务描述

实现判断卡版本化功能，支持v1/v2/v3版本管理，每次修正必须填写"推翻原因"，完整记录判断演进过程，防止经验流失。

## 🎯 任务目标

- 判断卡支持多版本
- 每次修改必须填写"推翻原因"
- 版本对比功能
- 使用历史记录

## 📐 技术方案

### 数据库扩展

```sql
-- 判断卡版本字段（已有，需确保）
ALTER TABLE judgement_cards ADD COLUMN version INTEGER DEFAULT 1;
ALTER TABLE judgement_cards ADD COLUMN is_current BOOLEAN DEFAULT TRUE;
ALTER TABLE judgement_cards ADD COLUMN previous_version_id UUID;

-- 变更记录扩展
ALTER TABLE jc_change_logs ADD COLUMN change_reason TEXT NOT NULL;
ALTER TABLE jc_change_logs ADD COLUMN overturned_by UUID REFERENCES users(id);
ALTER TABLE jc_change_logs ADD COLUMN overturned_at TIMESTAMPTZ;

-- 使用历史表
CREATE TABLE jc_usage_history (
    id BIGSERIAL PRIMARY KEY,
    jc_code VARCHAR(20) NOT NULL,
    jc_version INTEGER NOT NULL,
    ticket_id UUID REFERENCES tickets(ticket_id),
    used_by UUID REFERENCES users(id),
    used_at TIMESTAMPTZ DEFAULT NOW(),
    result VARCHAR(20),  -- 'correct', 'incorrect', 'partial'
    feedback TEXT
);
```

### 后端实现

**核心方法**：
```csharp
/// <summary>
/// 创建新版本（必须填写推翻原因）
/// </summary>
Task<JudgementCard> CreateNewVersionAsync(
    string jcCode,
    JudgementCardUpdateRequest request,
    string changeReason);  // 必须填写

/// <summary>
/// 版本对比
/// </summary>
Task<VersionComparison> CompareVersionsAsync(
    string jcCode,
    int version1,
    int version2);
```

## ✅ 验收标准

- [ ] 判断卡支持版本管理
- [ ] 修改时必须填写"推翻原因"
- [ ] 可以查看版本历史
- [ ] 可以对比不同版本
- [ ] 使用历史可追溯
- [ ] 版本成功率可统计

## 🔗 相关文档

- [v2.1需求清单 - REQ-002](../docs/PRD_V2.1_REQUIREMENTS.md#req-002-判断轨迹版本化decision-history)



