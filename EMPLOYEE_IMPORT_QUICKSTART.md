# 🚀 员工批量导入快速启动指南

本指南将帮助您快速上手员工批量导入功能。

---

## 📋 前提条件

### 1. 运行数据库迁移

在使用员工导入功能前，必须先运行数据库迁移脚本：

```bash
# 方法1：使用psql命令
cd /home/user/field-ticket-system/backend/scripts
psql -h localhost -U app -d fieldticket -f 003_add_employee_fields.sql

# 方法2：使用Docker (如果数据库运行在Docker中)
docker exec -i fieldticket-postgres psql -U app -d fieldticket < 003_add_employee_fields.sql
```

### 2. 启动后端服务

```bash
cd /home/user/field-ticket-system/backend
dotnet run --project src/FieldTicket.Api/FieldTicket.Api.csproj
```

后端服务将在 `http://localhost:5000` 启动。

### 3. 启动Web管理端

```bash
cd /home/user/field-ticket-system/web-admin
npm install  # 首次运行需要安装依赖
npm run dev
```

Web管理端将在 `http://localhost:5173` 启动。

---

## 🎯 使用流程

### 第1步：登录管理端

1. 访问：`http://localhost:5173`
2. 使用管理员账户登录：
   - 用户名：`admin`
   - 密码：`Admin123!` (首次登录需修改密码)

### 第2步：批量导入员工

#### 2.1 访问导入页面

在左侧菜单中，选择：**用户管理 → 员工批量导入**

或直接访问：`http://localhost:5173/users/import`

#### 2.2 下载Excel模板

1. 点击"下载员工导入模板"按钮
2. 将下载名为 `员工导入模板.xlsx` 的文件

#### 2.3 填写员工信息

在Excel模板中填写以下信息：

| 列名 | 是否必填 | 说明 | 示例 |
|------|---------|------|------|
| 姓名 | ✅ 必填 | 员工姓名，将作为登录账号 | 张三 |
| 身份证号 | ✅ 必填 | 至少4位，用于生成密码 | 110101199001011234 |
| 部门名称 | ✅ 必填 | 员工所属部门 | 技术部 |
| 部门号 | ⭕ 可选 | 部门编号 | DEPT001 |
| 上级姓名 | ⭕ 可选 | 直属上级的姓名 | 李四 |
| 手机号 | ⭕ 可选 | 联系电话 | 13800138000 |
| 邮箱 | ⭕ 可选 | 电子邮箱 | zhangsan@example.com |
| 角色 | ⭕ 可选 | 默认为FieldEngineer | FieldEngineer |

**填写示例：**

```
| 姓名 | 身份证号 | 部门名称 | 部门号 | 上级姓名 | 手机号 | 邮箱 | 角色 |
|------|---------|---------|--------|---------|---------|------|------|
| 张三 | 110101199001011234 | 技术部 | DEPT001 | 李四 | 13800138000 | zhangsan@example.com | FieldEngineer |
| 王五 | 110101199002022345 | 销售部 | DEPT002 | 赵六 | 13900139000 | wangwu@example.com | CS |
```

#### 2.4 上传文件导入

1. 在导入页面，选择默认角色（可选）
2. 勾选"覆盖已存在的用户"（可选）
3. 点击"选择并上传Excel文件"
4. 等待导入完成

#### 2.5 查看导入结果

导入完成后，页面将显示：
- ✅ **成功数量**：成功导入的员工数
- ❌ **失败数量**：导入失败的员工数
- ⏭️ **跳过数量**：已存在而跳过的员工数

**成功列表中显示：**
- 姓名
- 登录账号（用户名）
- **初始密码**（可复制）⭐ 重要！
- 部门
- 上级
- 角色

**⚠️ 重要：请复制并保存初始密码，通知员工使用！**

**密码生成规则：** `姓名拼音 + 身份证后4位`

示例：
- 张三 + 1234 → `zhangsan1234`
- 王五 + 2345 → `wangwu2345`

### 第3步：开通账户

#### 3.1 访问开通页面

在左侧菜单中，选择：**用户管理 → 账户开通审核**

或直接访问：`http://localhost:5173/users/activate`

或在导入结果页面点击："前往账户开通页面"

#### 3.2 审核员工信息

页面将显示所有未开通的账户，包括：
- 姓名
- 登录账号
- 部门
- 上级
- 手机号
- 邮箱
- 角色
- 导入时间

#### 3.3 开通账户

**方式1：单个开通**
1. 点击员工右侧的"开通"按钮
2. 在确认对话框中点击"确认开通"

**方式2：批量开通**
1. 勾选需要开通的员工（可使用"全选"）
2. 点击顶部的"批量开通"按钮
3. 在确认对话框中点击"确认开通"

开通成功后，员工即可使用登录账号和初始密码登录系统。

### 第4步：通知员工登录

将以下信息通知给员工：

```
【账户开通通知】

您好，${姓名}！

您的系统账户已开通，请使用以下信息登录：

🌐 登录地址：http://localhost:5173
👤 登录账号：${登录账号}
🔐 初始密码：${初始密码}

⚠️ 重要提示：
1. 首次登录后，系统会要求您修改密码
2. 新密码要求：至少8位，必须包含大小写字母和数字
3. 请妥善保管您的账户信息

如有问题，请联系系统管理员。
```

---

## 🔐 员工登录流程

### 1. 首次登录

1. 访问：`http://localhost:5173`
2. 输入登录账号和初始密码
3. 点击"登录"

### 2. 强制修改密码

首次登录后，系统会自动跳转到修改密码页面：

1. 输入旧密码（初始密码）
2. 输入新密码（至少8位，包含大小写字母和数字）
3. 确认新密码
4. 点击"修改密码"

### 3. 正常使用

密码修改成功后，即可正常使用系统。

---

## 📊 用户管理

### 查看所有用户

访问：**用户管理 → 用户管理**

或直接访问：`http://localhost:5173/users`

在这里可以：
- 查看所有用户列表
- 查看用户的开通状态（未开通/已开通）
- 直接开通未激活的账户
- 编辑用户信息
- 重置用户密码
- 禁用/启用用户
- 删除用户

### 开通状态说明

| 状态 | 颜色 | 说明 | 能否登录 |
|------|------|------|---------|
| 未开通 | 🟠 橙色 | 导入后未激活 | ❌ 不能 |
| 已开通 | 🟢 绿色 | 已激活可使用 | ✅ 可以 |

---

## ❓ 常见问题

### Q1: 导入后为什么员工无法登录？

**A:** 导入的账户默认处于"未开通"状态，需要管理员在"账户开通审核"页面手动开通后才能登录。

---

### Q2: 如何知道员工的初始密码？

**A:** 初始密码在导入成功后会显示在"成功列表"中，可以直接复制。密码生成规则为：`姓名拼音 + 身份证后4位`。

示例：
- 张三，身份证后4位1234 → 密码：`zhangsan1234`
- 李明华，身份证后4位5678 → 密码：`liminghua5678`

---

### Q3: 忘记了某个员工的初始密码怎么办？

**A:** 可以在"用户管理"页面找到该员工，点击"重置密码"按钮，为其设置新密码。

---

### Q4: 如何批量导入大量员工？

**A:**
1. 在Excel模板中填写所有员工信息
2. 一次性上传文件
3. 在"账户开通审核"页面使用"批量开通"功能

建议分批导入（每批100-200人）以便于管理。

---

### Q5: 导入时提示"身份证号不能为空"？

**A:** 身份证号是必填项，且至少需要4位（用于生成密码）。如果是测试数据，可以使用简单数字如"1234"。

---

### Q6: 上级关系为什么没有正确建立？

**A:** 请确保：
1. 上级用户已存在于系统中，或
2. 上级用户在同批次Excel中，且在上级的行在员工的行之前

系统会自动处理同批次导入的上级关系。

---

### Q7: 如何修改已导入员工的信息？

**A:**
- **方法1**：在"用户管理"页面找到该员工，点击"编辑"
- **方法2**：重新导入，勾选"覆盖已存在的用户"

---

### Q8: 企业微信用户和密码登录用户有什么区别？

**A:**
- **企业微信用户**：通过企业微信OAuth2登录，默认已开通
- **密码登录用户**：使用账号密码登录，导入后需要手动开通

---

### Q9: 如何查看所有未开通的账户？

**A:** 访问"账户开通审核"页面，或在"用户管理"页面筛选开通状态为"未开通"。

---

### Q10: 可以撤销账户开通吗？

**A:** 当前版本不支持直接撤销开通，但可以：
1. 在"用户管理"页面禁用该账户（切换状态开关）
2. 或删除该账户

---

## 📚 相关文档

- **完整使用指南**：`EMPLOYEE_IMPORT_GUIDE.md`
- **Web管理端启动指南**：`WEB_ADMIN_QUICK_START.md`
- **登录指南**：`LOGIN_GUIDE.md`
- **部署指南**：`DEPLOYMENT_GUIDE.md`

---

## 📞 技术支持

如有问题，请：
1. 查看完整使用指南：`EMPLOYEE_IMPORT_GUIDE.md`
2. 检查浏览器控制台错误信息
3. 检查后端API日志
4. 联系系统管理员

---

## 🎉 快速验证

完成上述步骤后，您可以：

### ✅ 验证导入功能
1. 访问 `http://localhost:5173/users/import`
2. 下载模板
3. 填写1-2个测试员工
4. 上传并查看结果

### ✅ 验证开通功能
1. 访问 `http://localhost:5173/users/activate`
2. 查看未开通账户列表
3. 开通一个账户

### ✅ 验证员工登录
1. 访问 `http://localhost:5173`
2. 使用员工账号和初始密码登录
3. 修改密码
4. 正常使用系统

---

**祝您使用愉快！** 🎊
