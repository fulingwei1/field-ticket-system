# 🧪 员工批量导入功能 - 测试指南

本文档提供完整的测试用例和测试流程，用于验证员工批量导入功能的正确性。

---

## 📋 目录

1. [测试环境准备](#测试环境准备)
2. [功能测试用例](#功能测试用例)
3. [边界情况测试](#边界情况测试)
4. [性能测试](#性能测试)
5. [安全测试](#安全测试)
6. [用户体验测试](#用户体验测试)
7. [测试数据集](#测试数据集)

---

## 🔧 测试环境准备

### 前置条件

- [x] 数据库已启动并可访问
- [x] 已运行数据库迁移脚本 (`003_add_employee_fields.sql`)
- [x] 已创建管理员账户 (运行`init-admin.csx`)
- [x] 后端API服务已启动 (`http://localhost:5000`)
- [x] Web管理端已启动 (`http://localhost:5173`)

### 环境验证清单

```bash
# 1. 检查数据库连接
psql -h localhost -U app -d fieldticket -c "SELECT version();"

# 2. 检查Users表结构
psql -h localhost -U app -d fieldticket -c "\d \"Users\""

# 3. 验证新字段存在
psql -h localhost -U app -d fieldticket -c "
  SELECT column_name, data_type
  FROM information_schema.columns
  WHERE table_name = 'Users'
  AND column_name IN ('DeptName', 'SupervisorId', 'SupervisorName', 'IdCardLastFour', 'IsActivated');"

# 4. 检查后端API是否运行
curl http://localhost:5000/health

# 5. 检查管理员账户
psql -h localhost -U app -d fieldticket -c "SELECT \"Username\", \"Role\", \"IsActivated\" FROM \"Users\" WHERE \"Role\" = 'Admin';"
```

---

## 🎯 功能测试用例

### 测试用例 1: 正常导入流程

**目标：** 验证基本的员工导入功能

**测试步骤：**
1. 使用管理员账户登录 (`admin` / `Admin123!`)
2. 访问"员工批量导入"页面 (`/users/import`)
3. 下载Excel模板
4. 填写3-5个员工信息（使用正常数据）
5. 上传Excel文件
6. 检查导入结果显示

**期望结果：**
- ✅ 成功导入所有员工
- ✅ 显示成功数量统计
- ✅ 显示生成的用户名和密码
- ✅ 密码符合规则（姓名拼音+身份证后4位）

**验证命令：**
```sql
-- 检查导入的用户
SELECT "Username", "Name", "DeptName", "SupervisorName", "IsActivated", "CreatedAt"
FROM "Users"
WHERE "LoginType" = 'Password'
ORDER BY "CreatedAt" DESC
LIMIT 10;
```

---

### 测试用例 2: 账户开通流程

**目标：** 验证账户开通功能

**测试步骤：**
1. 导入3个员工（参考测试用例1）
2. 访问"账户开通审核"页面 (`/users/activate`)
3. 查看未开通账户列表
4. 单个开通第1个账户
5. 批量开通第2和第3个账户
6. 刷新页面验证

**期望结果：**
- ✅ 未开通列表正确显示导入的员工
- ✅ 单个开通成功，账户状态变为"已开通"
- ✅ 批量开通成功，2个账户状态都变为"已开通"
- ✅ 开通后，员工可以登录系统

**验证命令：**
```sql
-- 检查开通状态
SELECT "Username", "Name", "IsActivated"
FROM "Users"
WHERE "LoginType" = 'Password'
ORDER BY "CreatedAt" DESC;
```

---

### 测试用例 3: 员工登录和密码修改

**目标：** 验证员工可以使用生成的密码登录

**测试步骤：**
1. 导入并开通一个员工账户（例如：张三）
2. 记录生成的密码（例如：`zhangsan1234`）
3. 退出管理员账户
4. 使用员工账户登录（`张三` / `zhangsan1234`）
5. 系统应要求修改密码
6. 设置新密码（例如：`NewPass123!`）
7. 使用新密码重新登录

**期望结果：**
- ✅ 使用初始密码登录成功
- ✅ 强制跳转到修改密码页面
- ✅ 密码修改成功
- ✅ 使用新密码登录成功
- ✅ `MustChangePassword`标志被清除

---

### 测试用例 4: 上下级关系

**目标：** 验证上下级关系正确建立

**测试步骤：**
1. 准备Excel文件，包含以下数据：
   - 李四（经理，无上级）
   - 张三（员工，上级：李四）
   - 王五（员工，上级：李四）
2. 上传并导入
3. 检查数据库中的上下级关系

**期望结果：**
- ✅ 张三的`SupervisorId`指向李四的`Id`
- ✅ 张三的`SupervisorName`为"李四"
- ✅ 王五的`SupervisorId`指向李四的`Id`
- ✅ 李四的`SupervisorId`为NULL

**验证命令：**
```sql
-- 查看上下级关系
SELECT
    u."Username" as "员工",
    u."Name" as "姓名",
    u."SupervisorName" as "上级姓名",
    s."Username" as "上级账号"
FROM "Users" u
LEFT JOIN "Users" s ON u."SupervisorId" = s."Id"
WHERE u."LoginType" = 'Password'
ORDER BY u."CreatedAt" DESC;
```

---

### 测试用例 5: 覆盖导入

**目标：** 验证覆盖模式是否正确工作

**测试步骤：**
1. 导入员工"张三"（部门：技术部）
2. 修改Excel中张三的部门为"销售部"
3. 勾选"覆盖已存在的用户"选项
4. 再次上传导入
5. 检查张三的信息

**期望结果：**
- ✅ 张三的部门被更新为"销售部"
- ✅ 其他字段也相应更新
- ✅ 用户ID保持不变
- ✅ 密码不变（除非重新生成）

---

## 🔍 边界情况测试

### 测试用例 6: 必填字段验证

**目标：** 验证必填字段的校验逻辑

**测试数据：**

| 姓名 | 身份证号 | 部门名称 | 预期结果 |
|------|---------|---------|---------|
| （空） | 1234 | 技术部 | ❌ 失败：姓名不能为空 |
| 张三 | （空） | 技术部 | ❌ 失败：身份证号不能为空 |
| 张三 | 123 | 技术部 | ❌ 失败：身份证号至少4位 |
| 张三 | 1234 | （空） | ❌ 失败：部门名称不能为空 |
| 张三 | 1234 | 技术部 | ✅ 成功 |

**期望结果：**
- ✅ 导入结果显示失败列表
- ✅ 错误信息清晰明确
- ✅ 显示失败的行号

---

### 测试用例 7: 特殊字符处理

**目标：** 验证特殊字符的处理

**测试数据：**

| 姓名 | 身份证号 | 部门 | 预期密码 | 备注 |
|------|---------|------|---------|------|
| 欧阳娜娜 | 1234 | 技术部 | ouyanangnana1234 | 复姓 |
| 李明-华 | 5678 | 销售部 | liminghua5678 | 连字符 |
| 王&五 | 9012 | 客服部 | wangwu9012 | 特殊符号 |
| John Smith | 3456 | 工程部 | johnsmith3456 | 英文名 |
| 张三 (张) | 7890 | 管理层 | zhangsan7890 | 括号 |

**期望结果：**
- ✅ 所有数据成功导入
- ✅ 密码生成符合预期
- ✅ 特殊字符被正确处理

---

### 测试用例 8: 重复数据处理

**目标：** 验证重复数据的处理逻辑

**测试步骤：**
1. 导入员工"张三"
2. 在同一Excel中再次包含"张三"
3. 上传导入（不勾选覆盖选项）

**期望结果：**
- ✅ 第一个张三成功导入
- ✅ 第二个张三被跳过
- ✅ 跳过数量显示为1
- ✅ 提示信息清晰

---

### 测试用例 9: 空行处理

**目标：** 验证Excel中空行的处理

**测试数据：**
```
行1: 标题行
行2: 张三的数据
行3: （完全空白）
行4: 李四的数据
行5: （部分空白，只有姓名）
行6: 王五的数据
```

**期望结果：**
- ✅ 行3（完全空白）被自动跳过
- ✅ 行5（部分空白）导入失败并提示错误
- ✅ 张三、李四、王五成功导入

---

### 测试用例 10: 大批量导入

**目标：** 验证系统处理大量数据的能力

**测试数据量：**
- 小批量：10个员工
- 中批量：100个员工
- 大批量：500个员工
- 超大批量：1000个员工

**测试步骤：**
1. 准备各批量的Excel文件
2. 依次导入并记录时间
3. 检查导入结果的准确性

**期望结果：**
- ✅ 所有批量都能成功处理
- ✅ 导入时间合理（100个<10秒）
- ✅ 数据准确性100%
- ✅ 不出现超时或崩溃

**性能基准：**
| 数量 | 期望时间 |
|------|---------|
| 10 | < 2秒 |
| 100 | < 10秒 |
| 500 | < 30秒 |
| 1000 | < 60秒 |

---

## 🔐 安全测试

### 测试用例 11: 权限控制

**目标：** 验证只有管理员可以访问导入功能

**测试步骤：**
1. 使用非管理员账户登录（例如：FieldEngineer角色）
2. 尝试访问 `/users/import`
3. 尝试访问 `/users/activate`
4. 尝试直接调用导入API

**期望结果：**
- ✅ 页面显示403无权限提示
- ✅ 自动重定向到首页
- ✅ API调用返回403错误
- ✅ 提示信息友好

---

### 测试用例 12: 文件类型验证

**目标：** 验证只接受Excel文件

**测试文件：**
- ❌ PDF文件
- ❌ Word文件
- ❌ TXT文件
- ❌ ZIP文件
- ✅ .xlsx文件
- ✅ .xls文件

**期望结果：**
- ✅ 非Excel文件上传失败
- ✅ 错误提示清晰
- ✅ Excel文件正常处理

---

### 测试用例 13: SQL注入测试

**目标：** 验证系统防SQL注入

**测试数据：**
```
姓名: Robert'; DROP TABLE Users; --
身份证号: 1234
部门: 技术部
```

**期望结果：**
- ✅ 数据被当作普通字符串处理
- ✅ 不执行任何SQL命令
- ✅ Users表完好无损

---

## 🎨 用户体验测试

### 测试用例 14: 错误提示友好性

**目标：** 验证错误提示的可读性

**测试场景：**
1. 上传非Excel文件
2. Excel格式错误
3. 必填字段缺失
4. 网络错误

**期望结果：**
- ✅ 所有错误都有中文提示
- ✅ 提示信息准确指出问题
- ✅ 提供修复建议
- ✅ 显示行号和字段名

---

### 测试用例 15: 加载状态反馈

**目标：** 验证加载状态的显示

**测试步骤：**
1. 上传较大的Excel文件（100+员工）
2. 观察上传过程
3. 观察处理过程

**期望结果：**
- ✅ 显示"导入中..."状态
- ✅ 按钮变为不可点击
- ✅ 有明确的加载指示
- ✅ 处理完成后立即显示结果

---

### 测试用例 16: 导入结果展示

**目标：** 验证导入结果的展示

**检查项：**
- ✅ 统计卡片清晰（成功/失败/跳过）
- ✅ 成功列表包含所有必要信息
- ✅ 密码可以一键复制
- ✅ 失败列表包含错误原因
- ✅ 可以查看每个员工的详细信息

---

## 📊 测试数据集

### 正常数据集 (test-normal.csv)

位置：`/home/user/field-ticket-system/backend/scripts/test-data/test-normal.csv`

包含10个正常员工数据，覆盖：
- 不同部门
- 不同角色
- 上下级关系
- 完整字段

### 边界数据集 (test-edge-cases.csv)

位置：`/home/user/field-ticket-system/backend/scripts/test-data/test-edge-cases.csv`

包含各种边界情况：
- 最长/最短姓名
- 特殊字符
- 复姓
- 英文名
- 数字和符号

### 错误数据集 (test-errors.csv)

位置：`/home/user/field-ticket-system/backend/scripts/test-data/test-errors.csv`

包含各种错误情况：
- 空字段
- 无效格式
- 重复数据
- 不存在的上级

---

## ✅ 测试检查清单

### 导入前检查
- [ ] 数据库已运行并可访问
- [ ] 迁移脚本已执行
- [ ] 管理员账户已创建
- [ ] 后端服务正常运行
- [ ] Web管理端正常访问

### 功能测试
- [ ] 正常导入流程（测试用例1）
- [ ] 账户开通流程（测试用例2）
- [ ] 员工登录测试（测试用例3）
- [ ] 上下级关系（测试用例4）
- [ ] 覆盖导入（测试用例5）

### 边界测试
- [ ] 必填字段验证（测试用例6）
- [ ] 特殊字符处理（测试用例7）
- [ ] 重复数据处理（测试用例8）
- [ ] 空行处理（测试用例9）
- [ ] 大批量导入（测试用例10）

### 安全测试
- [ ] 权限控制（测试用例11）
- [ ] 文件类型验证（测试用例12）
- [ ] SQL注入测试（测试用例13）

### 用户体验测试
- [ ] 错误提示友好性（测试用例14）
- [ ] 加载状态反馈（测试用例15）
- [ ] 导入结果展示（测试用例16）

---

## 📝 测试报告模板

```markdown
# 员工批量导入功能测试报告

## 测试信息
- 测试日期：2025-XX-XX
- 测试人员：[姓名]
- 测试环境：[开发/测试/生产]

## 测试结果摘要
- 通过用例数：X / 16
- 失败用例数：X
- 阻塞问题数：X

## 详细测试结果

### 测试用例1: 正常导入流程
- 状态：✅ 通过 / ❌ 失败
- 备注：[问题描述或特殊情况]

### 测试用例2: 账户开通流程
- 状态：✅ 通过 / ❌ 失败
- 备注：[问题描述或特殊情况]

...

## 发现的问题
1. [问题描述]
   - 严重程度：严重/一般/轻微
   - 重现步骤：
   - 期望结果：
   - 实际结果：

## 建议
- [改进建议1]
- [改进建议2]

## 测试结论
- [ ] 功能符合要求，建议上线
- [ ] 存在问题，需要修复后重测
- [ ] 存在严重问题，不建议上线
```

---

## 🚀 快速测试命令

```bash
# 1. 重置测试环境（清空测试数据）
psql -h localhost -U app -d fieldticket -c "
DELETE FROM \"Users\"
WHERE \"LoginType\" = 'Password'
AND \"Username\" != 'admin';"

# 2. 查看所有导入的用户
psql -h localhost -U app -d fieldticket -c "
SELECT \"Username\", \"Name\", \"DeptName\", \"IsActivated\", \"CreatedAt\"
FROM \"Users\"
WHERE \"LoginType\" = 'Password'
ORDER BY \"CreatedAt\" DESC;"

# 3. 查看未开通账户数量
psql -h localhost -U app -d fieldticket -c "
SELECT COUNT(*) as \"未开通账户数\"
FROM \"Users\"
WHERE \"IsActivated\" = false;"

# 4. 开通所有未开通账户（用于快速测试）
psql -h localhost -U app -d fieldticket -c "
UPDATE \"Users\"
SET \"IsActivated\" = true
WHERE \"IsActivated\" = false;"
```

---

**测试愉快！** 🎉
